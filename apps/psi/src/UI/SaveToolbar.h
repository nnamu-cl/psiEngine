#pragma once

/**
 * SaveToolbar - Small floating icon button for saving the current project.
 *
 * Positioned just below the brand label at the top center of the viewport.
 * The button is disabled when no project is loaded.
 */
class SaveToolbar
{
public:
    void Render();
};
